const cursoJS = [
    "Intro a JS",
    "Variable y tipos",
    "Conversión de tipos",
    "Funciones"
]

let countFor = 0;

for ( let i = cursoJS.length; i > 0; i--) {

    countFor += 1;
    console.log(`${countFor}. ${cursoJS[i-1]}`);
    

    
}
